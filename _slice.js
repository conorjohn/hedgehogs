/**
 * 
 * 
 * @param { Array } array 
 * @param { Number } startPos 
 * @param { String } endPos 
 * 
 * @return { Array } returns a subset of the passed array from the starting index, up to but not including the last index
 */
const slice = (array, startPos, endPos) => {

}

module.exports = {
    slice: slice
}